[pytest]
norecursedirs = volumes airflow logs tmp node_modules .git platform/tests/system
markers =
    unit: Unit tests (small, isolated)
    integration: Integration tests (DB, ES, etc.)
    system: End-to-end system tests (full pipeline or multiple services)
    smoke: Smoke tests for basic health checking
